# Smilo – AI-Powered Dental Disease Detection System
## Complete End-to-End Functional Workflow and Application Structure

The Smilo AI-Powered Dental Disease Detection System is a comprehensive web-based healthcare platform that begins when a user lands on the Home Page (Index.tsx at route "/") featuring a full-screen animated hero section with the headline "AI-Powered Dental Disease Detection – Your Smile, Our Priority" accompanied by a call-to-action button "Start Free Analysis" and background animations using Framer Motion showing floating dental icons or gradient effects, followed by scrollable sections including "How It Works" explaining the three-step process with illustrated cards showing Step 1 Upload Your OPG X-ray with upload icon, Step 2 AI Analyzes in Seconds with brain/processing animation, and Step 3 Get Instant Report with document icon alongside timing estimates, a "Why Choose Smilo" section displaying benefit cards highlighting 95%+ Accuracy from trained AI models, Instant Results within 30 seconds, Expert Verification by real dentists, Secure & Private with HIPAA compliance badges, and 24/7 Availability, a Features section with animated icons showing Dental Caries Detection, Heatmap Visualization, Personalized Guidance, and Doctor Network, a Doctors preview section teasing the verified dentist network with "Connect with 500+ Verified Dentists" and a "View All Doctors" link, a testimonials carousel showing user reviews with star ratings and photos, a statistics counter animating numbers like "10,000+ X-rays Analyzed", "500+ Dentists", "95% Accuracy Rate", a prominent CTA section with "Ready to Check Your Dental Health?" and buttons for "Upload X-ray Now" and "Learn More", and a comprehensive footer containing company information, quick links, social media icons, privacy policy, terms of service, and contact details, all connected through a persistent animated Navbar component (Navbar.tsx) that remains fixed at the top of every page featuring the Smilo logo with a tooth icon on the left serving as a clickable home link, horizontal navigation menu items (Home, How It Works, Detect Disease, Doctors, About, Contact) styled with smooth hover effects using Tailwind CSS with transition properties and active state underlines using border-bottom animations, a responsive hamburger menu icon for mobile devices that toggles a slide-in drawer using Shadcn Sheet component containing the same navigation links stacked vertically, and a dynamic authentication section on the right side that for unauthenticated users displays two buttons styled as outlined "Login" and solid "Register" using Shadcn Button variants, while for authenticated users shows a user avatar with dropdown menu (Shadcn DropdownMenu) triggered on click revealing options "Dashboard" (icon: LayoutDashboard), "My Reports" (icon: FileText), "Profile Settings" (icon: Settings), a divider, and "Logout" (icon: LogOut) with red text color, where the Navbar detects authentication state from a React Context (AuthContext) providing user data including name, email, role, and avatar URL fetched from the backend after login verification, and highlights the current active page by checking useLocation().pathname from React Router DOM applying an active class with different text color and bottom border, implementing the authentication flow where any unauthenticated user clicking "Upload X-ray Now", "Start Free Analysis", "Detect Disease" from navbar, or any protected action triggers a navigation to "/auth" route rendering the Auth.tsx page component featuring a centered card layout with tabs or toggle between "Login" and "Register" views using Shadcn Tabs component, where the Login tab displays a form with email input field validated for proper email format, password input field with show/hide toggle icon, a "Remember me" checkbox, a "Forgot Password?" link aligned right opening a password reset modal with email input, a prominent "Login" button that on click validates the form using React Hook Form with Zod schema checking required fields and formats, shows loading state with spinner replacing button text during API call, submits credentials to POST /api/auth/login endpoint sending JSON body {email, password}, receives response containing JWT access token, refresh token, and user object {userId, name, email, role, avatar, city}, stores the access token in localStorage under key 'authToken' or in a httpOnly cookie if backend is configured to set it, stores user data in AuthContext state making it available globally, displays success toast notification using Sonner showing "Welcome back, [Name]!", determines redirect target based on user role where role 'user' redirects to "/dashboard" and role 'doctor' redirects to "/doctor-dashboard", and uses navigate() from useNavigate with replace option preventing back button to login page, while the Register tab displays a comprehensive signup form with fields for full name (validated for minimum 2 words), email (validated for format and uniqueness checked on blur via debounced API call), phone number (validated for format with country code dropdown), city selection (Shadcn Select component populated from a cities array like Karachi, Lahore, Islamabad, Rawalpindi, Faisalabad), password (validated for minimum 8 characters, uppercase, lowercase, number, special character with visual strength indicator showing weak/medium/strong), confirm password (validated to match password field), role selection with radio buttons or select defaulting to "User" with option for "Doctor" that when selected shows additional conditional fields including specialization dropdown (General Dentistry, Orthodontics, Periodontics, Endodontics, Oral Surgery), years of experience number input, clinic name text input, clinic address textarea, license number input, and document upload for verification certificate using file input accepting PDF/JPG limited to 5MB, an "I agree to Terms & Conditions" checkbox required for form submission, and a "Create Account" button that validates the complete form showing inline error messages beneath each field with red text and error icons, uploads verification documents if role is doctor to /api/uploads/verification endpoint first returning document URLs, then submits all registration data to POST /api/auth/register endpoint sending JSON including name, email, hashedPassword (hashed on backend with bcrypt), phone, city, role, and doctor-specific fields if applicable, where the backend creates a new user record in the Users table with initial verified status false for doctors requiring admin approval and true for regular users, sends a verification email containing a magic link or OTP code, returns success response with message "Account created successfully! Please verify your email", displays success toast and automatically switches to Login tab with email pre-filled, implementing the User Dashboard accessed at "/dashboard" route protected by PrivateRoute wrapper component that checks if authToken exists in localStorage and user is authenticated in AuthContext else redirects to "/auth?redirect=/dashboard", rendering the Dashboard.tsx component with a responsive grid layout featuring a header section with welcome message "Welcome back, [User Name]!" with greeting based on time of day, user avatar with edit icon overlay allowing profile picture change, and last login timestamp, a statistics cards row displaying three cards using Shadcn Card component showing "Total Uploads" with count from database query and upload icon, "Reports Generated" with count and document icon, "Consultations Booked" with count and calendar icon, each card animated with hover scale effect and showing percentage change compared to last month with green up arrow or red down arrow, a prominent quick actions section with large cards or buttons for "Upload New X-ray" (primary action with gradient background and camera icon) navigating to "/upload", "View All Reports" navigating to "/reports", "Find Doctors" navigating to "/doctors", and "Book Consultation" opening a modal with consultation booking form, a "Recent Activity" section displaying a timeline or list using Shadcn ScrollArea showing the 5 most recent activities with icons, timestamps, and descriptions like "X-ray uploaded for analysis", "AI Report generated - Dental Caries Detected", "Consultation booked with Dr. Ahmed", "Report downloaded", fetched from uploads and consultations tables ordered by date descending, an "Upload History" section with a data table using Shadcn Table component showing columns for thumbnail preview (small image from xrayImageUrl), upload date formatted as "DD MMM YYYY, HH:mm", status badge (Shadcn Badge) with colors like yellow for "Processing", green for "Completed", red for "Failed", "View Report" button (Shadcn Button size sm variant outline) enabled only for completed uploads navigating to "/results/:resultId", and action dropdown menu with options to download, delete, or share, with pagination controls at bottom if more than 10 records exist, and a sidebar section with profile summary card showing name, email, phone, city with "Edit Profile" button opening a modal with editable fields, account settings link, help center link, and notifications toggle, implementing the complete X-ray upload workflow where clicking "Upload New X-ray" navigates to "/upload" route rendering the Upload.tsx page component featuring a centered layout with progress steps indicator at top showing Step 1 Select Image, Step 2 Preview & Validate, Step 3 Submit for Analysis with current step highlighted, a large drag-and-drop zone using react-dropzone library or custom implementation with onDrop handler, styled as a dashed border box with upload cloud icon and text "Drag & drop your OPG X-ray here, or click to browse" supporting file types JPEG, PNG, DICOM with validation checking file.type against allowed MIME types ['image/jpeg', 'image/png', 'application/dicom'], file size against maximum 10MB showing error toast if exceeded, and basic image dimension check ensuring minimum 500x500 pixels using Image onLoad to get naturalWidth and naturalHeight, displaying selected file information including name, size formatted as human-readable (e.g., "2.4 MB"), and file type icon, showing an instant preview of the uploaded image in a larger container with zoom controls (+ and - buttons) using transform scale CSS, rotation buttons (rotate left/right) using CSS transform rotate, brightness and contrast sliders (Shadcn Slider) applying CSS filters for image adjustment helping users ensure quality before analysis, fullscreen view button opening image in a modal overlay for detailed inspection, a validation checklist section with checkboxes auto-checked based on validation rules showing requirements like "Image format is supported" with green check or red cross, "Image is clear and not blurry" with manual checkbox requiring user confirmation, "File size is within limit", "Image shows full panoramic view", "No personal information visible on image", an optional metadata form section with fields for patient age range select (0-10, 11-20, 21-30, 31-40, 41-50, 51+), symptoms experienced multi-select checkboxes (tooth pain, sensitivity, bleeding gums, bad breath, loose teeth, none), last dental visit date picker using Shadcn Calendar, and additional notes textarea, a terms acknowledgment section with checkbox and text "I understand this is an AI screening tool and not a replacement for professional dental diagnosis", validation showing inline errors if required checks are not completed, and prominent action buttons row with "Cancel" button (variant outline) navigating back to dashboard, "Reset" button clearing the selected image and form, and "Analyze X-ray" button (variant default with primary color, size lg) disabled until all validations pass and terms are acknowledged, which onClick handler triggers submitAnalysis async function that shows loading state replacing button text with spinner and "Uploading...", creates a FormData object appending the image file, metadata fields, userId from AuthContext, and timestamp, makes POST request to /api/uploads endpoint with multipart/form-data content type and authorization header "Bearer ${token}", receives response containing uploadId and initial status "processing", displays success toast "X-ray uploaded successfully! AI analysis in progress...", stores the uploadId in component state, starts a polling mechanism using setInterval calling GET /api/uploads/:uploadId endpoint every 3 seconds to check status, updates UI with progress messages cycling through "Preprocessing image...", "Running AI analysis...", "Generating heatmap...", "Preparing report..." with animated progress bar using Shadcn Progress component showing indeterminate state, when status changes to "completed" clears the interval, retrieves resultId from response, navigates to "/results/:resultId" with navigate and passes state data, or if status is "failed" shows error modal with message and option to retry or contact support, implementing the backend upload endpoint where Express.js POST /api/uploads route first verifies JWT token from authorization header using middleware extracting userId and role, uses Multer middleware to handle multipart file upload with storage configuration for diskStorage or memoryStorage, validates file mimetype in fileFilter callback rejecting non-image files, saves the uploaded file temporarily, generates unique filename using uuid combined with original extension preventing collisions, uploads image to AWS S3 bucket or Google Cloud Storage using official SDK (aws-sdk or @google-cloud/storage) to folder structure like "uploads/{userId}/{uploadId}/original.jpg" with public-read ACL or presigned URL generation for security, creates new record in Uploads table with columns uploadId (UUID primary key), userId (foreign key to Users), xrayImageUrl (S3 URL), uploadedAt (timestamp), status (enum: pending/processing/completed/failed), metadata (JSONB storing age, symptoms, notes), returns {uploadId, status, message} response, triggers asynchronous AI processing by publishing message to Redis queue or RabbitMQ or directly calling AI microservice endpoint POST /api/ai/analyze with payload {uploadId, imageUrl, userId}, implementing the AI microservice as a separate Python Flask or FastAPI application running on different port or as AWS Lambda function or Google Cloud Function, exposing POST /analyze endpoint that accepts {uploadId, imageUrl, userId} as JSON, downloads the image from provided URL using requests library saving to temporary directory, validates image is readable using PIL/Pillow with Image.open() catching exceptions, preprocesses the image through pipeline including resizing to model input size (224x224 for MobileNet, 512x512 for ResNet) using Image.resize() with antialias, converting to RGB array using np.array(), normalizing pixel values to range [0,1] or [-1,1] depending on model requirement using /255.0 or ImageNet normalization with mean=[0.485, 0.456, 0.406] and std=[0.229, 0.224, 0.225], expanding dimensions using np.expand_dims() to create batch dimension resulting in shape (1, H, W, 3), loading the pre-trained deep learning model which is a Convolutional Neural Network architecture like ResNet50, VGG16, EfficientNetB3, or custom CNN trained using TensorFlow/Keras or PyTorch on a labeled dataset of dental X-ray images containing classes ["Healthy", "Dental_Caries"] with data augmentation techniques (rotation, flip, brightness adjustment, zoom) during training achieving validation accuracy of 93-97%, the model loaded at application startup using tf.keras.models.load_model('model.h5') or torch.load('model.pth') kept in memory for fast inference, runs prediction by calling model.predict(preprocessed_image) for TensorFlow returning softmax probabilities array like [0.13, 0.87] or model(torch.tensor(preprocessed_image)) for PyTorch, extracts predicted class index using np.argmax(predictions), maps index to class label ("Healthy" or "Dental_Caries"), calculates confidence score as the maximum probability percentage (87.3%), determines severity level based on confidence ranges where 60-70% is "Mild", 70-85% is "Moderate", 85-100% is "Severe" for caries or "None" for healthy, generates visual explanation using Grad-CAM (Gradient-weighted Class Activation Mapping) technique by accessing last convolutional layer outputs using TensorFlow GradientTape or PyTorch hooks, computing gradients of predicted class score with respect to feature maps, performing global average pooling of gradients, multiplying feature maps by pooled gradients as weights, summing weighted feature maps to create heatmap, applying ReLU to remove negative values, resizing heatmap to original image dimensions, normalizing heatmap values to range [0, 255], applying colormap using cv2.applyColorMap with cv2.COLORMAP_JET creating red-yellow gradient where red indicates high importance regions showing areas influencing caries prediction (typically cavity locations, decay spots, abnormal density), overlaying heatmap on original image with transparency alpha=0.4 using cv2.addWeighted creating final visualization image, uploading heatmap image to S3 bucket at "uploads/{userId}/{uploadId}/heatmap.jpg" obtaining URL, preparing comprehensive analysis result object containing {uploadId, prediction: class_label, confidence: confidence_score, severity: severity_level, heatmapUrl, processedAt: timestamp, modelVersion: "v1.2.3", affectedRegions: array of bounding boxes if object detection used, precautions: curated array based on prediction and severity, dietGuidance: array of dietary recommendations, oralCareGuidance: array of oral hygiene tips}, where precautions array is populated based on severity using conditional logic like for Severe Caries ["Schedule urgent dental appointment within 48 hours", "Avoid chewing on affected side", "Take over-the-counter pain relief if needed", "Rinse with warm salt water", "Avoid extremely hot or cold foods"], for Moderate Caries ["Schedule dental appointment within 1-2 weeks", "Maintain gentle brushing around affected area", "Use fluoride toothpaste", "Avoid sticky and sugary foods", "Consider using sensitivity toothpaste"], for Mild Caries ["Monitor the condition closely", "Schedule routine dental checkup within 1 month", "Increase brushing frequency to after every meal", "Use fluoride mouthwash daily", "Reduce sugar intake"], for Healthy ["Continue regular dental checkups every 6 months", "Maintain good oral hygiene routine", "No immediate action required", "Keep monitoring your dental health"], dietGuidance array containing recommendations like ["Reduce sugar consumption from candies, sodas, and desserts", "Limit acidic foods and drinks like citrus fruits, tomatoes, vinegar", "Avoid sticky foods like caramel, dried fruits, and gummy candies", "Increase calcium-rich foods like milk, cheese, yogurt for tooth strength", "Consume crunchy vegetables like carrots, celery for natural cleaning", "Drink plenty of water especially after meals to wash away food particles", "Choose whole grains over refined carbohydrates", "Eat foods rich in vitamin D and phosphorus for enamel health", "Limit snacking between meals to reduce acid attacks", "Chew sugar-free gum with xylitol after eating"], oralCareGuidance array with best practices ["Brush teeth twice daily for 2 minutes using proper technique - circular motions, 45-degree angle to gums", "Use soft-bristled toothbrush replaced every 3 months", "Floss daily to remove plaque and food between teeth where brush cannot reach", "Use antimicrobial or fluoride mouthwash after brushing", "Clean your tongue daily to remove bacteria causing bad breath", "Consider using electric toothbrush for more effective cleaning", "Avoid tobacco products which stain teeth and cause gum disease", "Limit alcohol consumption which dries mouth reducing saliva protection", "Schedule professional dental cleaning every 6 months", "Apply fluoride treatment or dental sealants as recommended by dentist", "Use interdental brushes for wider gaps between teeth", "Rinse mouth with water after consuming sugary or acidic foods"], sends results back to main backend by calling webhook POST /api/ai/callback endpoint with authorization key for security passing complete analysis result JSON, implementing the backend callback endpoint where POST /api/ai/callback verifies the authorization key or HMAC signature ensuring request is from trusted AI service, extracts uploadId from payload, creates new record in Results table with columns resultId (UUID primary key), uploadId (foreign key unique), userId (foreign key), prediction (varchar), confidenceScore (decimal), severityLevel (varchar), heatmapUrl (text), processedAt (timestamp), modelVersion (varchar), precautions (JSONB array), dietGuidance (JSONB array), oralCareGuidance (JSONB array), affectedRegions (JSONB), updates corresponding Uploads record setting status='completed' and resultId=new_resultId, triggers notification sending in-app notification record to Notifications table with message "Your X-ray analysis is ready!" linked to resultId, sends push notification if user has enabled and provided FCM token, sends email notification using SendGrid or AWS SES with subject "Your Smilo Dental Analysis Report is Ready" containing summary and view report link, optionally assigns the case to nearby verified doctors in user's city for professional review by creating records in CaseAssignments table linking resultId with doctorIds where doctor city matches user city and doctor specialization is relevant, returns success response {status: 'success', resultId}, implementing the Results page accessed at "/results/:resultId" route protected by PrivateRoute, rendering Results.tsx component that on mount extracts resultId from useParams(), makes GET request to /api/results/:resultId endpoint with authorization header, backend verifies the requesting userId matches the result's userId preventing unauthorized access to others' reports or checks if user role is 'doctor' and has assigned access, returns complete result data including all stored fields plus original X-ray URL fetched via join with Uploads table, component renders comprehensive report layout with header section showing report ID, generation timestamp formatted as "Generated on DD MMM YYYY at HH:mm", download PDF button (icon: Download) that triggers client-side PDF generation using jsPDF library or calls backend GET /api/results/:resultId/download endpoint which uses Puppeteer or Playwright to render HTML template of report and convert to PDF buffer streaming as response with content-type 'application/pdf' and content-disposition 'attachment; filename=report.pdf', share button opening modal with options to share via email (input email and send), WhatsApp (opens WhatsApp web with pre-filled message and report link), or copy link to clipboard, a visual comparison section with two-column layout using Shadcn Card showing left side "Original X-ray" with image from xrayImageUrl in Shadcn AspectRatio container maintaining proper dimensions with zoom on hover using CSS transform scale and cursor zoom-in opening fullscreen modal on click, right side "AI Analysis Heatmap" showing heatmapUrl image with legend explaining color coding (red = high confidence areas, yellow = moderate, blue = low concern), a diagnosis card prominently centered with conditional styling based on prediction where "Healthy" shows green background with CheckCircle icon and message "Great news! No dental issues detected", "Dental_Caries" shows orange/red background with AlertTriangle icon and message "Dental Caries Detected - Professional consultation recommended", displaying confidence score as large percentage number with Shadcn Progress radial component or circular progress indicator, severity level badge using Shadcn Badge with color variants (slate for None, yellow for Mild, orange for Moderate, red for Severe), a detailed findings section with Shadcn Accordion allowing expand/collapse showing AI explanation in simple language "The AI model detected potential cavity formation in the upper right molar region (tooth #15). The area shows increased density and darkening characteristic of decay" with technical details tab showing model version, processing time, confidence breakdown per class, a precautions section with Shadcn Alert component variant="warning" listing all precautions from database array mapped to list items with icon bullets using CheckCircle for actionable items, urgency indicator if severity is high showing red pulsing dot and text "Urgent: Seek dental care immediately", a comprehensive guidance section with Shadcn Tabs containing three tabs "Diet Recommendations", "Oral Care", "Prevention Tips", Diet Recommendations tab showing dietGuidance array items as cards with food icons and descriptions organized into "Foods to Avoid" (red section) and "Foods to Include" (green section), Oral Care tab displaying oralCareGuidance items as numbered list with illustrations or icons showing brushing technique animations using Lottie or CSS animations, Prevention Tips tab with general long-term advice for maintaining dental health, a "Consult a Doctor" section prominently placed with gradient background highlighting importance, explaining "For accurate diagnosis and treatment planning, we recommend consulting a verified dentist" with list of benefits like "Professional examination", "Customized treatment plan", "Insurance coverage support", "Follow-up care", featuring primary button "Find Dentists Near You" (size lg) that onClick navigates to "/doctors" with URL parameter "?city={userCity}&filter=verified&specialty=general" pre-filtering results, secondary button "Request Video Consultation" opening booking modal with telemedicine option, a history tracking section if user has multiple reports showing comparison graph using Recharts library displaying confidence scores over time as line chart, upload dates on x-axis, allowing users to track dental health progress, action buttons footer with "Save to Dashboard" (auto-saved but can manually sync), "Download PDF Report", "Share with Doctor" opening modal to select doctor from consultations or doctor list sending report access permission, "Upload New X-ray" navigating back to upload page, "Back to Dashboard" navigating to /dashboard, implementing the Doctors page accessible via "/doctors" route as public page accessible to all users rendering Doctors.tsx component with hero section "Find Verified Dentists Near You" and subtitle explaining network quality, a comprehensive filter and search section using Shadcn Command or custom form with inputs for search by name using text input with debounced onChange, filter by city using Select component populated from database distinct cities with default value from user's city if authenticated, filter by specialization using multi-select Checkboxes for General Dentistry, Orthodontics, Periodontics, Endodontics, Oral Surgery, Cosmetic Dentistry, filter by availability with Radio Group options "Available Today", "Available This Week", "Any Time", filter by rating using Slider component setting minimum stars 3-5, filter by consultation type Checkboxes for In-Clinic, Video Consultation, Home Visit, sort by dropdown with options Recommended, Highest Rated, Most Experienced, Nearest Location, Price Low to High, with "Apply Filters" button triggering GET /api/doctors endpoint with query parameters constructing Sequelize/Prisma where clause, "Reset Filters" button clearing all filter state, displaying results count "Showing 24 of 156 dentists", results section showing grid layout (3 columns on desktop, 2 on tablet, 1 on mobile) using CSS Grid, each doctor card using Shadcn Card with hover elevation effect, displaying doctor profile picture in circular Avatar, verification badge icon (ShieldCheck) if verified is true, name as heading with link to profile, specialization text with icon, clinic name and address truncated with tooltip showing full text on hover, city with location pin icon, rating displayed as stars using lucide-react Star icons filled based on average rating (e.g., 4.5 stars) with review count in parentheses "(127 reviews)", experience shown as "15+ years experience", availability status badge showing "Available Today" in green or "Booked" in gray with next available slot timing "Next: Tomorrow 10:00 AM", languages spoken with flag icons, consultation fee displayed as "Rs. 2,000 - 3,000" or "Video: Rs. 1,500", action buttons row with "View Profile" (variant outline) opening doctor profile modal or navigating to "/doctors/:doctorId", "Book Appointment" (variant default primary) opening booking modal with calendar date picker using Shadcn Calendar, time slot selection showing available slots fetched from GET /api/doctors/:doctorId/availability with radio group of time buttons, consultation type radio selection, option to attach report from user's history if authenticated using Select component populated from user's completed results, notes textarea for symptoms description, patient contact confirmation showing pre-filled phone and email with edit option, total consultation fee display, "Confirm Booking" button making POST /api/consultations request creating consultation request with status 'pending', showing success confirmation modal with appointment details and confirmation code, implementing the doctor profile modal or detail page showing cover photo of clinic, profile section with larger avatar, name with verified badge and specialization, clinic information card with name, full address, phone number, email, website link, operating hours table, photos gallery of clinic interior using Shadcn Carousel, about section with biography text, education and credentials section listing degrees with institutions and years, certifications with issuing bodies, professional registrations with license numbers, services offered as tagged badges, specializations list, patient reviews section with Shadcn ScrollArea showing review cards with patient name or anonymous, rating stars, date posted, review text, doctor's reply if any, helpful votes count, pagination or load more button if many reviews, map section with placeholder or iframe showing Google Maps with clinic location marker with future enhancement note "Interactive map coming soon", appointment booking section sticky to sidebar with date and time selection, implementing the backend doctors endpoint GET /api/doctors with query parameters parsing req.query extracting city, specialization, availability, minRating, consultationType, search, sortBy, page, limit, constructing database query using Sequelize findAll or Prisma findMany with where clause building conditions like {city: {[Op.like]: `%${city}%`}, specialization: {[Op.in]: specializationArray}, verified: true, rating: {[Op.gte]: minRating}}, joins with Users table to get user details, includes Consultations table to check availability by counting consultations for date ranges, orders by sortBy parameter mapping to columns like rating DESC, experience DESC, created_at DESC, implements pagination with offset: (page-1)*limit and limit, returns JSON {doctors: array, total: count, page, totalPages: Math.ceil(count/limit)}, implementing Doctor Authentication where doctors register through the same /auth page but select role="doctor" providing additional verification documents, registration creates user with verified=false, doctor profile entry in Doctors table linking userId, admin reviews submitted documents manually or through automated verification service, updates verified=true enabling doctor login and dashboard access, doctor login flow checks verified status in response, if not verified shows modal "Your account is pending verification. You'll receive email once approved" preventing dashboard access, if verified successful login redirects to "/doctor-dashboard", implementing the Doctor Panel at "/doctor-dashboard" route protected by RoleBasedRoute checking user.role === 'doctor' else redirecting to user dashboard, rendering DoctorDashboard.tsx component with fixed layout structure using flex container where left sidebar (DoctorSidebar.tsx component) has fixed width 250px with Shadcn ScrollArea containing navigation menu, top section showing doctor's avatar with name, specialization text, online status indicator (green dot) with text "Online", menu items list using Shadcn NavigationMenu or custom nav with items array [{icon: LayoutDashboard, label: 'Overview', path: '/doctor-dashboard'}, {icon: Users, label: 'Patient Reports', path: '/doctor-dashboard/reports', badge: pendingCount}, {icon: Image, label: 'X-ray Viewer', path: '/doctor-dashboard/xrays'}, {icon: Brain, label: 'AI Predictions', path: '/doctor-dashboard/ai-predictions'}, {icon: FileText, label: 'Case History', path: '/doctor-dashboard/cases'}, {icon: MessageSquare, label: 'Feedback to AI', path: '/doctor-dashboard/feedback'}, {icon: Calendar, label: 'Appointments', path: '/doctor-dashboard/appointments', badge: upcomingCount}, {icon: User, label: 'My Profile', path: '/doctor-dashboard/profile'}, {icon: Settings, label: 'Settings', path: '/doctor-dashboard/settings'}], each item rendered as NavLink from React Router with activeClassName styling to highlight current page using bg-primary/10 text-primary, icons from lucide-react positioned left of label text, optional badge component showing count positioned right for Reports and Appointments, bottom section with "Back to Main Site" link, "Help Center" link, logout button, right content area with flex-1 taking remaining width containing top header bar with breadcrumb navigation showing current page path using Shadcn Breadcrumb, notification icon button with Badge showing unread count opening dropdown of recent notifications, doctor avatar dropdown menu with quick access to profile and settings, main content area with padding rendering nested routes using Outlet, implementing the Overview page (default /doctor-dashboard) showing welcome message "Good [morning/afternoon/evening], Dr. [Name]", dashboard summary cards grid with "Pending Reports" card showing count with link to reports page icon FileText, "Consultations Today" card with count and icon Calendar, "Total Patients" lifetime count icon Users, "AI Accuracy Rate" showing percentage of confirmed AI predictions with icon CheckCircle, each card with colored icon background, count as large number, label text, and trend indicator showing change from previous period with up/down arrow and percentage in green/red text, quick actions section with buttons "View Pending Reports", "Check Schedule", "Add Availability Slots", "Update Profile", recent activity timeline showing chronological list with timestamps like "New report assigned - Patient ID #12345", "Consultation completed with Patient #12289", "AI prediction validated for Case #445", each with icon and relative time "2 hours ago", statistics section with charts using Recharts library displaying "Monthly Consultations" as Bar Chart with months on x-axis, "Diagnosis Distribution" as Pie Chart showing healthy vs caries percentages, "AI Validation Trends" as Line Chart showing agree vs disagree over time, upcoming appointments section showing next 3 appointments with patient ID, scheduled time, consultation type badge, "Join Video Call" button if video consultation time is within 10 minutes using meeting link, implementing the Patient Reports page at "/doctor-dashboard/reports" showing filter tabs using Shadcn Tabs with "Pending Review" (default active), "Reviewed", "All Cases", filter controls with date range picker using Shadcn DateRangePicker, severity filter multi-select, AI prediction filter, city filter, search by patient ID, data table using Shadcn Table with columns Patient ID (clickable), Upload Date (sortable), AI Prediction (badge colored green for Healthy, red for Caries), Confidence Score (percentage with color coding <70% yellow, 70-85% orange, >85% red), Severity (badge), Status (badge: Pending, Under Review, Completed), Actions (view button), rows clickable opening report detail view, pagination with page size selector, implementing the report detail view when clicking any row navigating to "/doctor-dashboard/reports/:resultId" rendering ReportDetail.tsx with back button to reports list, two-panel layout with left panel showing patient information section (Patient ID, upload date, age range, reported symptoms, contact option if consultation booked), X-ray images section with tabs for Original and Heatmap showing large images with zoom controls, previous reports history if patient has prior uploads showing list with dates and predictions collapsible section allowing comparison, right panel showing AI analysis summary section displaying prediction, confidence score with visual indicator, severity level badge, affected regions description, model version used, processing timestamp, heatmap interpretation explaining highlighted areas, doctor's review section with status indicator (Not Reviewed, Under Review by Dr. [Name], Reviewed on [Date]), if not reviewed by current doctor shows "Add Your Review" button expanding review form, review form containing "Professional Diagnosis" radio group with options Confirm AI Prediction, Correct to Healthy, Correct to Caries, additional option for Other Dental Conditions textarea, "Clinical Notes" rich text editor (using Tiptap or similar) allowing formatted text with sections for Findings, Diagnosis, Treatment Recommendation, Follow-up Plan, "Severity Assessment" dropdown if correcting prediction selecting None/Mild/Moderate/Severe, "Recommended Treatment" multi-select checkboxes (Filling, Root Canal, Extraction, Crown, Monitoring, Preventive Care, Referral to Specialist), "Prescription" textarea with formatting for medication names, dosage, duration, "Follow-up Timeline" date picker with radio options (1 week, 2 weeks, 1 month, 3 months, 6 months, custom date), "Confidence in Diagnosis" slider 1-10 helping doctor indicate certainty level, "Flag for AI Training" checkbox allowing doctor to mark this case for model retraining if AI was significantly wrong, "Priority Level" select (Routine, Moderate, Urgent) affecting patient notification tone, "Internal Notes" textarea for doctor's private notes not shared with patient, "Save Review" button making POST /api/doctor/reviews endpoint creating DoctorReviews record with reviewId, resultId, doctorId, diagnosis, clinicalNotes, treatmentPlan, followUpDate, confidenceLevel, flaggedForTraining, reviewed at timestamp, updating Results table with doctorValidated=true doctorDiagnosis=diagnosis, triggering notification to patient "Your report has been reviewed by Dr. [Name]", updating case status to completed, action buttons for "Print Report", "Download PDF", "Share with Patient" sending them access link via email or SMS, "Request Consultation" if not already booked creating consultation request, implementing the X-ray Viewer page at "/doctor-dashboard/xrays" showing dedicated interface for viewing all X-ray images with sidebar list of all reports filtered by date with thumbnails and patient IDs, main viewer area displaying selected X-ray with advanced controls including zoom slider, pan using mouse drag, rotate buttons, brightness and contrast sliders, measurement tools (ruler overlay for measuring distances if calibration data available), annotation tools (draw arrows, circles, text labels) saving annotations as overlay data, compare mode showing two X-rays side-by-side for same patient or different patients, fullscreen mode button, image quality assessment showing metadata like resolution, file size, format, export image button downloading with or without annotations, implementing the AI Predictions page at "/doctor-dashboard/ai-predictions" showing analytics dashboard focused on AI model performance with filters for date range, prediction type, severity, confidence range, statistics cards showing "Total AI Predictions" count, "Validation Rate" percentage of predictions reviewed by doctors, "Agreement Rate" percentage where doctor confirmed AI prediction, "Disagreement Rate" where doctor corrected, "Average Confidence Score", charts section with "Prediction Accuracy by Severity" bar chart comparing AI vs doctor diagnosis grouped by severity level, "Confidence Distribution" histogram showing count of predictions at different confidence ranges, "Temporal Trends" line chart showing agreement rate over months indicating model improvement, data table listing all predictions with columns AI Prediction, Doctor Diagnosis, Agreement Status (match/mismatch), Confidence Score, Review Date, Doctor Name, with filters and sorting, ability to click rows opening full report detail, insights section showing AI model strengths (e.g., "Highly accurate for severe caries: 96% agreement") and areas for improvement (e.g., "Moderate cases show higher false positives: 68% agreement"), implementing the Case History page at "/doctor-dashboard/cases" showing comprehensive list of all cases reviewed by the doctor with advanced filtering by date range, diagnosis type, patient demographics, treatment provided, follow-up status, search by patient ID or case ID, summary statistics showing total cases reviewed, distribution pie chart of diagnoses, average review time per case, export functionality to download case list as CSV or Excel including selected columns for reporting purposes, individual case cards or table rows showing patient ID, date reviewed, final diagnosis, treatment recommended, follow-up date if scheduled, current status (Treatment Pending, In Progress, Completed), last update timestamp, click to view full case details opening modal with all information, implementing the Feedback to AI page at "/doctor-dashboard/feedback" providing interface for doctors to specifically flag cases where AI made errors helping improve model through active learning, showing form with case selection dropdown populated from reviewed cases, feedback type radio group (False Positive, False Negative, Incorrect Severity, Missed Region, Other), detailed explanation textarea requiring doctor to describe what AI got wrong and what correct diagnosis should be, image annotation tool allowing doctor to mark specific regions AI missed or incorrectly identified, severity rating of AI error (Minor, Moderate, Major), suggestion for model improvement textarea, submit button sending feedback to POST /api/ai/feedback endpoint storing in AIFeedback table linking doctorId, resultId, feedbackType, description, annotationData, submittedAt, optional automatic trigger creating support ticket for data science team to review, feedback history section showing previously submitted feedback with status (Submitted, Under Review, Implemented in Model v1.x, Rejected with reason), implementing the Appointments page at "/doctor-dashboard/appointments" showing calendar view using Shadcn Calendar or custom implementation with appointments rendered as colored blocks on dates, list view tab showing table of appointments with columns Patient ID, Date & Time, Consultation Type (In-Clinic, Video Call), Status (Confirmed, Pending, Completed, Cancelled), attached report link if patient shared X-ray report, actions (View Details, Start Video Call, Cancel, Reschedule), filter controls for upcoming appointments, past appointments, cancelled appointments, status filters, appointment detail modal showing patient information (name, age, contact), booked date and time, consultation type, patient's chief complaints from notes, attached X-ray report with quick view button, doctor's previous notes if follow-up appointment, action buttons (Confirm Appointment, Request Reschedule, Cancel with reason, Mark as Completed, Add to Calendar exporting ICS file), for video consultations showing "Join Call" button enabled 5 minutes before scheduled time opening video interface using embedded Twilio Video or Zoom SDK with in-app video player, chat feature, screen sharing for discussing X-rays, record consultation option, availability management section allowing doctor to set weekly schedule with day-wise time slot configuration, mark holidays or leaves blocking dates, set consultation duration default (e.g., 30 minutes), set maximum appointments per day preventing overbooking, enable/disable online booking toggle, implementing the My Profile page at "/doctor-dashboard/profile" showing editable doctor profile with sections for personal information (name, email, phone with edit buttons opening inline forms or modals), professional information (specialization, sub-specializations multi-select, years of experience number input, about me rich textarea), clinic information (clinic name, address with autocomplete, city, map marker placement, phone, email, website URL, photos gallery upload with drag-drop multiple images stored in S3), credentials (education entries with degree, institution, year adding multiple with + button, certifications with name, issuer, issue date, certificate upload, license numbers with verification status), services offered (checkboxes for various dental procedures from preset list plus custom entries), languages spoken (multi-select from language list), consultation fees (separate inputs for in-clinic fee, video consultation fee, follow-up visit fee, home visit fee with checkbox to enable each type), availability schedule (weekly calendar grid with checkboxes for time slots Monday-Sunday, special hours for holidays), verification documents section showing uploaded documents with status badges, option to upload additional documents, profile preview button showing how profile appears to patients on doctors listing page, save changes button updating database, implementing the Settings page at "/doctor-dashboard/settings" with sections for account settings (change password form, email preferences checkboxes for notification types like new appointment, report assigned, patient message, marketing emails, SMS notification toggle with phone verification), privacy settings (profile visibility radio: Public visible to all, Limited visible to booked patients only, Hidden not shown in listings, patient data access restrictions), notification preferences with granular controls for each notification type with toggles for email, SMS, push, in-app channels, appointment settings (auto-confirm appointments toggle, buffer time between appointments, advance booking limit in days, cancellation policy text), payment settings (bank account details for receiving consultation fees, payment methods accepted checkboxes, issue invoice automatically toggle), integration settings (calendar sync with Google Calendar or Outlook with OAuth connection button, telemedicine platform credentials, lab result integration API keys if supported), danger zone section with red border containing deactivate account button with confirmation modal explaining consequences, implementing the complete navigation flow where all sidebar menu items use React Router NavLink with 'to' prop pointing to respective paths, useLocation hook determines active path applying active styles, clicking menu items smoothly transitions content area using React Router Outlet rendering matched child route component with fade-in animation using Framer Motion AnimatePresence, breadcrumb dynamically updates based on current route using pathname split and map to create breadcrumb items linking back to parent pages, implementing the technical architecture where frontend React application built with Vite uses React Router DOM v6 with BrowserRouter wrapping App component defining Routes with nested routes structure like <Route path="/" element={<Layout />}> containing Navbar and Outlet rendering child routes <Route index element={<Index />} />, <Route path="auth" element={<PublicRoute><Auth /></PublicRoute>} />, <Route path="dashboard" element={<PrivateRoute><Dashboard /></PrivateRoute>} />, <Route path="upload" element={<PrivateRoute><Upload /></PrivateRoute>} />, <Route path="results/:resultId" element={<PrivateRoute><Results /></PrivateRoute>} />, <Route path="doctors" element={<Doctors />} />, <Route path="doctors/:doctorId" element={<DoctorProfile />} />, <Route path="doctor-dashboard" element={<RoleBasedRoute role="doctor"><DoctorLayout /></RoleBasedRoute>}> with nested child routes <Route index element={<DoctorOverview />} />, <Route path="reports" element={<PatientReports />} />, <Route path="reports/:resultId" element={<ReportDetail />} /> etc, Route Guards implemented as wrapper components PrivateRoute checking if localStorage.getItem('authToken') exists and AuthContext.isAuthenticated is true rendering children else navigating to "/auth" with redirect param using <Navigate to={`/auth?redirect=${location.pathname}`} replace />, RoleBasedRoute additionally checking AuthContext.user.role matches required role prop rendering children if authorized else redirecting to appropriate dashboard showing toast "Unauthorized access", PublicRoute for auth page checking if already authenticated redirecting logged-in users attempting to access login page back to their dashboard, AuthContext implemented using React createContext providing {user, isAuthenticated, login, logout, updateUser} functions used across components, login function stores token in localStorage, makes GET /api/auth/me request to fetch full user profile, sets user state, sets isAuthenticated true, logout function removes token from localStorage, calls POST /api/auth/logout, resets user state to null, resets isAuthenticated false, navigates to home, useEffect on App mount checks if token exists calling /api/auth/me to restore session maintaining login across page refreshes, Axios instance configured with baseURL pointing to backend API (http://localhost:5000/api in development, production API URL in build), request interceptor adding authorization header "Bearer ${token}" to every request, response interceptor catching 401 errors logging user out automatically on token expiration, backend Node.js Express application structure with server.js entry point setting up Express app, configuring middleware (express.json, express.urlencoded, cors with origin whitelist, helmet for security headers, compression for response compression, morgan for logging), connecting to PostgreSQL database using Sequelize ORM defining models for Users, Doctors, Uploads, Results, Consultations, DoctorReviews, Notifications, AIFeedback with proper associations like Users.hasMany(Uploads), Uploads.hasOne(Results), Results.hasMany(DoctorReviews), Users.hasOne(Doctors) for role-based segregation, database migrations for schema versioning and deployment, seeds for initial data like admin user and sample cities, routes organized in separate files (auth.routes.js, user.routes.js, upload.routes.js, result.routes.js, doctor.routes.js, consultation.routes.js, ai.routes.js) imported and mounted on app with respective prefixes, authentication implemented with JWT using jsonwebtoken library where login endpoint after password verification with bcrypt.compare generates token with jwt.sign({userId, email, role}, process.env.JWT_SECRET, {expiresIn: '7d'}) returning token and user object, auth middleware function extracting token from authorization header, verifying with jwt.verify, decoding payload, fetching user from database by userId, attaching user object to req.user available in route handlers, role middleware checking req.user.role against required role array throwing 403 Forbidden if not authorized, file uploads handled by Multer with memory storage or disk storage, files uploaded to AWS S3 using aws-sdk instantiating S3 client with credentials from environment variables, upload method with bucket name, key (file path), body (file buffer), ACL public-read or private with presigned URLs for access, returning S3 URL stored in database, AI service communication implemented as HTTP requests using Axios POST to AI service URL with retry logic using axios-retry library, or using message queue Redis with bull library creating analysis job queue where upload endpoint publishes job, AI service worker consumes jobs processing them asynchronously, completing jobs triggers callbacks, database queries optimized with indexes on foreign keys and frequently filtered columns like status, userId, city, createdAt, eager loading related data using Sequelize include option reducing N+1 queries, pagination implemented with findAndCountAll returning rows and count, API responses standardized with consistent structure {success: boolean, data: object/array, message: string, error: object with details if failed}, error handling middleware catching errors returning appropriate HTTP status codes 400 for validation, 401 for unauthorized, 403 for forbidden, 404 for not found, 500 for server errors, logging errors using Winston or Pino to files and external services like Sentry for production monitoring, environment variables managed using dotenv library loading from .env file containing sensitive configs like DATABASE_URL, JWT_SECRET, AWS_ACCESS_KEY_ID, AWS_SECRET_ACCESS_KEY, SMTP credentials, API URLs, frontend environment variables prefixed VITE_ accessed via import.meta.env, production deployment with frontend built using npm run build generating optimized static files in dist folder served by Nginx or deployed to Vercel/Netlify with automatic CDN, backend deployed to AWS EC2, Heroku, or containerized with Docker deployed on ECS/Kubernetes, database hosted on AWS RDS or managed PostgreSQL services like Supabase, AI service deployed as separate container or serverless function, implementing comprehensive security measures including input validation on all endpoints using express-validator with sanitization, SQL injection prevention using parameterized queries through ORM, XSS protection with helmet middleware setting CSP headers and escaping output, CSRF protection for state-changing requests using csurf middleware with tokens, rate limiting using express-rate-limit preventing brute force attacks on login endpoint limiting to 5 attempts per 15 minutes, secure password hashing with bcrypt using salt rounds 10, secure token storage recommending httpOnly cookies over localStorage for production to prevent XSS token theft, HTTPS enforcement in production with SSL certificates from Let's Encrypt, CORS properly configured allowing only trusted frontend origins, sensitive data encryption at rest for database fields like phone numbers using encryption libraries, audit logging for critical actions like login attempts, report access, diagnosis modifications storing in AuditLogs table with userId, action, resourceId, timestamp, IP address, user agent, HIPAA compliance considerations with data anonymization options, user consent tracking, data retention policies with automatic deletion after specified period, access controls ensuring users can only view their own data and doctors can only access assigned cases, implementing comprehensive testing strategy with unit tests for utility functions using Vitest, component tests for React components using React Testing Library checking rendering, user interactions, and prop handling, integration tests for API endpoints using Supertest making requests and asserting responses, E2E tests using Playwright or Cypress automating user flows from landing to registration to upload to results to booking covering happy paths and error scenarios, performance testing using tools like Artillery or k6 simulating concurrent users analyzing response times and identifying bottlenecks, implementing monitoring and analytics with application performance monitoring using New Relic or Datadog tracking response times, error rates, database query performance, user analytics using Google Analytics or Mixpanel tracking page views, user journeys, conversion funnels identifying where users drop off, custom events for critical actions like upload submitted, report viewed, appointment booked, error tracking using Sentry capturing frontend JavaScript errors and backend exceptions with stack traces, contextual information, user session data, uptime monitoring using services like UptimeRobot or StatusCake checking endpoint availability alerting on downtime, log aggregation using ELK stack (Elasticsearch, Logstash, Kibana) or cloud services like CloudWatch Logs centralizing logs from multiple services enabling search and visualization, implementing future enhancements roadmap including advanced AI features like multi-disease detection extending model to identify periodontal disease, gingivitis, impacted teeth, cysts, tumors requiring retraining with expanded labeled datasets, severity progression prediction using time-series analysis if user uploads X-rays over time, 3D X-ray support for CBCT scans requiring volumetric CNN models, automated tooth numbering system overlaying FDI notation on X-rays, telemedicine video consultations deeply integrated with live screen sharing of X-rays and annotations during calls, prescription management with e-prescription generation complying with medical standards and pharmacy integration, lab integration allowing doctors to order lab tests like biopsy, blood tests through platform with results delivery, treatment tracking where patients log treatment received and progress with before/after photo uploads, insurance integration with automatic claim submission and approval tracking, mobile apps using React Native sharing codebase with web providing native camera access for better X-ray capture quality and push notifications, internationalization supporting multiple languages like Urdu, Arabic, Spanish using i18next with JSON language files, blockchain for medical records storing immutable hash of reports on blockchain for verification and ownership, AI model marketplace allowing multiple AI models from different providers competing with selection based on accuracy and speed, referral system where general dentists can refer complex cases to specialists within platform, patient community features like forums for dental health discussions moderated by professionals, gamification with achievements and points for maintaining good oral hygiene encouraging regular checkups, integration with smart toothbrushes syncing brushing data providing personalized feedback, reminder system for brushing, flossing, checkups with customizable schedules, educational content library with articles, videos, infographics about dental health searchable and categorized, virtual dental assistant chatbot using NLP answering common questions about symptoms, procedures, costs providing preliminary guidance before consultation, ensuring the entire Smilo system provides a complete, professional, production-ready solution addressing all aspects of an AI-powered dental disease detection platform from initial user interest through authentication, AI analysis, professional validation, doctor consultation, and ongoing dental health management with clear separation of concerns between user and doctor roles, intuitive navigation through persistent navbar and role-specific dashboards with fixed sidebars, robust backend architecture handling authentication, authorization, file uploads, AI integration, database management, secure data handling following healthcare compliance standards, comprehensive frontend built with modern React patterns, responsive design, accessible components, smooth animations, and extensible architecture allowing future feature additions, making it suitable as a final year project demonstrating full-stack development skills, AI integration capabilities, healthcare domain knowledge, security awareness, and professional software engineering practices.
